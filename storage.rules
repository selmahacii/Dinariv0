rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Lecture publique des images ; écriture réservée aux utilisateurs connectés
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
